import { BackgroundSoundData } from "../../elements/BackgroundSound";
import { Room } from "../../elements/Playable";

// Vorlage für die Konfiguration eines Raum-Hintergrundsounds.

const backgroundSound: BackgroundSoundData = {
  filename: "lied.mp3",
  room: Room.YELLOW_ROOM,
};

export default backgroundSound;
